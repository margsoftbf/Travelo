const FilterBar = () => {
    return (
      <div className="p-4 border rounded shadow">
        {/* Tu dodasz filtry jak range slider itp. */}
        <p>Filter components here</p>
      </div>
    );
  };
  
  export default FilterBar;